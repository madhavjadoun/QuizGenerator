# nixpacks.toml
# Tells Railway/Nixpacks to install tesseract and poppler_utils for OCR and PDF parsing support.

[phases.setup]
nixpkgs = ["...", "tesseract", "poppler_utils"]
aptpkgs = ["...", "tesseract-ocr", "poppler-utils"]
